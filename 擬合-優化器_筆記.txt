欠擬合(Underfitting)
正確擬合(Just right)
過擬合(Overfitting)


===防止過擬合===
1.增加數據集
2.正則化方法
3.Dropout

=============================

各種優化器對比

標準梯度下降法:
標準梯度下降先計算所有樣本彙總誤差, 然根據總誤差來更新權值

隨機梯度下降法:
隨機梯度下降隨機抽取一個樣本來計算誤差, 然後更新權值

批量梯度下降法:
批量機度下降算是一種折衷的方案, 從總樣本中選取一個批次(比如一共有10000個樣本, 隨機取100個樣本作為一個batch),
然後計算這個batch的總務差, 根據總務差來更新權值